/**
 * Interface Quote
 */
export interface Quote {
  q: string, // quote text
  a: string, // author name
  i: string, // author image (key required)
  c: string, // character count
  h: string // pre-formatted HTML quote
}
